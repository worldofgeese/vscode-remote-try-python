kind: Module
type: container
name: vscode-remote-try-python
dockerfile: Dockerfile
services:
  - name: frontend
    ports: 
      - name: http
        containerPort: 5000
    ingresses:
      - port: http
        annotations:
          kubernetes.io/ingress.class: "nginx"
