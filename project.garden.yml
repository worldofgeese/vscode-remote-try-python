kind: Project
name: vscode-remote-try-python
defaultEnvironment: dev
variables:
  # change these values to your own
  username: worldofgeese
  # registryHostname: docker.io
  # registryNamespace: worldofgeese

environments:
  - name: dev
    defaultNamespace: py-hello-world-${variables.username}
  - name: prod
    defaultNamespace: py-hello-world
    production: true

providers:
  - name: local-kubernetes
    context: minikube
    # deploymentRegistry:
    #   hostname: "${var.registryHostname}"
    #   namespace: "${var.registryNamespace}"